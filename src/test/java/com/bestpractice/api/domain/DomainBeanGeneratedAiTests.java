package com.bestpractice.api.domain;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.extension.ExtendWith;
import static org.junit.jupiter.api.Assertions.assertThrows;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.BeforeAll;

import org.junit.Test;
import static com.bestpractice.api.domain.PasswordEncoder.PasswordEncoder;
import static com.bestpractice.api.domain.DomainBeanGeneratedAiTests.PasswordEncoder.PasswordEncoder;
import org.junit.Reset;
import org.junit.Test;

public class DomainBeanGeneratedAiTests {

    @Test
    public void testPasswordEncoderReturnsEncoder() {
        Reset.setContainer(PasswordEncoder.class);
        PasswordEncoder passwordEncoder = PasswordEncoder.new PasswordEncoder();
        PasswordEncoder.passwordEncoder();
        PasswordEncoder.passwordEncoder();
        assertEquals(PasswordEncoder.new PasswordEncoder().getPasswordEncoder(), passwordEncoder);
    }

    @Test
    public void testPasswordEncoderReturnsEmpty() {
        Reset.setContainer(PasswordEncoder.class);
        PasswordEncoder passwordEncoder = PasswordEncoder.new PasswordEncoder();
        passwordEncoder.passwordEncoder();
        passwordEncoder.passwordEncoder();
        assertEquals(null, passwordEncoder);
    }

    @Test
    public void testPasswordEncoderReturnsEmpty() {
        Reset.setContainer(PasswordEncoder.class);
        PasswordEncoder passwordEncoder = PasswordEncoder.new PasswordEncoder();
        passwordEncoder.passwordEncoder();
        passwordEncoder.passwordEncoder();
        assertEquals(null, passwordEncoder);
    }

    @Test
    public void testPasswordEncoderReturnsEmpty() {
        Reset.setContainer(PasswordEncoder.class);
        PasswordEncoder passwordEncoder = PasswordEncoder.new PasswordEncoder();
        passwordEncoder.passwordEncoder();
        passwordEncoder.passwordEncoder();
        assertEquals(null, passwordEncoder);
    }

    @Test
    public void testPasswordEncoderReturnsEmpty() {
        Reset.setContainer(PasswordEncoder.class);
        PasswordEncoder passwordEncoder = PasswordEncoder.new PasswordEncoder();
        passwordEncoder.passwordEncoder();
        passwordEncoder.passwordEncoder();
        assertEquals(null, passwordEncoder);
    }
}
