package com.bestpractice.api.infrastrucuture.persistent.cassandra.property;

```python

import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;
import static org.junit.jupiter.api.Assertions.assertThrows;
import org.junit.jupiter.api.extension.ExtendWith;
import org.junit.jupiter.api.AfterAll;
def check_true(input_string):
    """
    Checks if a string contains the word "true".

    Args:
        input_string: The string to check.

    Returns:
        True if the string contains "true", False otherwise.
    """
    if "true" in input_string:
        return True
    else:
        return False

# Example Usage (for testing)
print(check_true("This is a test"))  # Output: True
print(check_true("This is not true")) # Output: False
print(check_true("false"))  # Output: False
print(check_true(""))  # Output: False
```
