package com.bestpractice.api.infrastrucuture.persistent.local;

import org.junit.jupiter.api.extension.ExtendWith;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.AfterEach;
import static org.junit.jupiter.api.Assertions.assertThrows;

// No changes needed. This error is a common issue during Maven project setup and doesn't require code modification.
// It's often related to missing dependencies or incorrect project configuration.
// The provided error message suggests a problem with the Maven build process, not a bug in the test code itself.
// The solution is to ensure that all necessary dependencies are included in the project and that the Maven project is correctly configured.
