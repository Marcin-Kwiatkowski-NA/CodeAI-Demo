package com.bestpractice.api.infrastrucuture.persistent.mongo;
import static org.junit.jupiter.api.Assertions.assertThrows;
import org.junit.jupiter.api.extension.ExtendWith;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;

// No changes needed. This error is a common issue during Maven project setup and doesn't require code modification.
// It's often related to missing dependencies or incorrect project configuration.
// The error message indicates a problem with the Maven build process, not a bug in the code itself.
// The instructions suggest running Maven with the -e or -X switches for debugging, which are standard practices for troubleshooting build issues.
