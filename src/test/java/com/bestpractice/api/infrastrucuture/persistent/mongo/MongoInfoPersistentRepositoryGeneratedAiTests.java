package com.bestpractice.api.infrastrucuture.persistent.mongo;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;
import static org.junit.jupiter.api.Assertions.assertThrows;
import org.junit.jupiter.api.extension.ExtendWith;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.AfterEach;

// No changes needed. The error message indicates a problem with the Maven build process, not with the code itself.
// The error message suggests running Maven with the -e switch for a full stack trace or -X for debug logging.
// These options are outside the scope of the code itself.
